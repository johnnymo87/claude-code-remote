# Example configuration - DO NOT put real secrets here
# Secrets are managed via SecretSpec (see secretspec.toml)
#
# On devbox: secrets come from sops-nix (/run/secrets/*)
# On macOS: secrets stored in Keychain
#
# See .claude/skills/security/SKILL.md for details

# These have defaults in secretspec.toml but can be overridden:
# TELEGRAM_ENABLED=true
# TELEGRAM_CHAT_ID=your-chat-id
# TELEGRAM_WEBHOOK_PORT=4731
# CCR_WORKER_URL=https://ccr-router.your-account.workers.dev
# CCR_MACHINE_ID=your-machine-name
# LOG_LEVEL=info
